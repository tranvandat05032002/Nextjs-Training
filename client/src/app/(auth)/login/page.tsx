import Button from '@/app/Components/Button';
import React from 'react';

const LoginPage = () => {
    return (
        <div>
            this is page login
            <Button></Button>
        </div>
    );
};

export default LoginPage;